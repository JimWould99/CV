import React from "react";
import ReactDOM from "react-dom/client";
import CV_body from "./cv";

import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CV_body></CV_body>
  </React.StrictMode>
);
